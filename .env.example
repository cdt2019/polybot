# Telegram Configuration (Optional)
# 用于接收 Bot 运行状态和交易通知
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Polymarket API Credentials (Optional but Recommended)
# 如果不提供，Bot 会尝试从私钥派生（速度较慢且可能不稳定）
# 获取方式：在 Polymarket 官网 -> Settings -> Builder Codes 生成
CLOB_API_KEY=your_api_key
CLOB_SECRET=your_api_secret
CLOB_PASS_PHRASE=your_passphrase

# Wallet Configuration
# 必须提供私钥，用于签名交易
# 格式：0x 开头的 64 位十六进制字符串
PRIVATE_KEY=0x_your_private_key_here

# MONITOR Configuration
# 设置监控频率（单位为秒）
MONITOR_INTERVAL=60

# Order Configuration (Default Values)
# 这些值可以被命令行参数覆盖 (e.g., --orderSize=20)
# ORDER_SIZE: 下单数量 (USDC for Market Buy, Shares for Limit/Sell)
ORDER_SIZE=10
# ORDER_TYPE: 订单类型 (MARKET 或 LIMIT)
ORDER_TYPE=MARKET
# ORDER_PRICE: 价格限制 (MARKET：可以最大价格， LIMIT：指定价格)
ORDER_PRICE=0.9
# TIME_IN_FORCE: 订单有效期策略 (默认为 FAK)
# GTC: Good-Till-Cancel (一直有效直到取消, 仅限 LIMIT)
# FOK: Fill-Or-Kill (全部成交或取消, 仅限 MARKET)
# FAK: Fill-And-Kill (部分成交后取消剩余, 仅限 MARKET, 推荐用于防滑点)
# GTD: Good-Till-Date (指定时间后过期, 仅限 LIMIT)
TIME_IN_FORCE=FAK

# Google GenAI API Key, https://aistudio.google.com/api-keys
GOOGLE_API_KEY=your_google_api_key_here

# Polymarket CLOB 签名类型, 使用浏览器插件钱包登录polymarket使用 2
# 0: EOA (If you don't know what this is you're not using it)
# 1: Magic/Email Login
# 2: Browser Wallet(Metamask, Coinbase Wallet, etc)
POLYMARKET_SIGNATURE_TYPE="0"

# Polymarket 账户内的托管钱包地址
# This is the address you deposit/send USDC to to FUND your Polymarket account.
# This is the address listed below your profile picture when using the Polymarket site.
POLYMARKET_FUNDER_ADDRESS="0x_your_funder_address"   
